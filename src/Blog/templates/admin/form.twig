{{ field('title', 'Titre *', item.title, {placeholder: 'Enter le titre', col:12}) }}
{{ field('slug', 'Lien *', item.slug, {placeholder: 'Entrer le lien'}) }}
{{ field('created_date', 'Date de création', item.created_date, {class:'datepicker'}) }}
{{ field('content', 'Contenu *', item.content, {type: 'textarea', col: 12, placeholder: 'Contenu'}) }}